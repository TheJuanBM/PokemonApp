import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  ContainerLoading: {
    margin: 16,
    alignItems: 'center',
    justifyContent: 'center'
  },
  Loading: {
    height: 60
  },
  ActivityIndicator: {
    zIndex: -1,
    position: 'absolute'
  }
})
