import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  TextInput: {
    flex: 1,
    height: 40,
    padding: 10,
    elevation: 10,
    borderRadius: 10,
    color: '#000000',
    backgroundColor: 'white'
  }
})
